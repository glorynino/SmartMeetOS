# =========================
# SmartMeetOS (Render + Supabase)
# =========================

# Supabase Postgres
DATABASE_URL=

# Groq
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
GROQ_BASE_URL=https://api.groq.com/openai/v1

# App state directory
# On Render, point this to your persistent disk mount path (recommended)
SMARTMEETOS_STATE_DIR=/var/data

# Google Calendar OAuth
# Recommended for Render: store these as Render environment variables.
# Provide either JSON or base64.
GOOGLE_CLIENT_SECRET_JSON=
# GOOGLE_CLIENT_SECRET_B64=

# Provide a refreshable token JSON (generated locally once) so Render doesn't need a browser login.
GOOGLE_TOKEN_JSON=
# GOOGLE_TOKEN_B64=

# Set to 1 in production so we never attempt to open a browser for OAuth.
SMARTMEETOS_NONINTERACTIVE=1

# Nylas
NYLAS_API_KEY=
NYLAS_API_BASE=https://api.us.nylas.com
NYLAS_GRANT_ID=
