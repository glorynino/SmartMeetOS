"""Agents layer.

In the original big-project layout, agents orchestrate calls into `services/`.
This repo's working meeting automation lives under `smartmeetos/`.

The modules here provide a thin integration layer so the larger app can import a
stable entrypoint.
"""
