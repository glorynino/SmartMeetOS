"""Scheduling agent integration.

This module is the natural place to:
- query Google Calendar
- decide which meetings to join
- trigger the notetaker supervisor

Currently, the fully working implementation is the CLI `check_calendar.py`.
"""
