"""Operational helper scripts.

These are optional CLI/tools used during development and operations (status checks, transcript
merge/backfill, webhook receiver).

They are kept under `services/tools/` to avoid cluttering the repo root.
"""
