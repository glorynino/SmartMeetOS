"""Service-layer wrappers.

The original SmartMeetOS implementation in this repo lives under the `smartmeetos/` package.
This `services/` package exists to integrate that working implementation into the larger
project structure (agents/services/webapp).

These modules should stay thin: they delegate to `smartmeetos.*` where possible.
"""
